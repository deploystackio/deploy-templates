services:
  - name: default
    type: web
    runtime: image
    image:
      url: docker.io/nbr23/youtube-dl-server:latest
    startCommand: ""
    plan: starter
    region: oregon
    envVars:
      - key: PORT
        value: "8080"
