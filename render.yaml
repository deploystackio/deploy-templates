services:
  - name: default
    type: web
    runtime: image
    image:
      url: ghcr.io/chartdb/chartdb:latest
    startCommand: ""
    plan: starter
    region: oregon
    envVars:
      - key: PORT
        value: "80"
